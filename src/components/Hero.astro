<div class='hero'>
    <h1>Nowoczesne strony internetowe</h1>
    <p>Oferujemy nowoczesne strony, które wyróżniają się na tle konkurencji.</p>
    <img src='/hero.svg' alt='website advertising' />
</div>

<style>
    .hero {
        display: flex;
        flex-direction: column;
        align-items: center;
        justify-content: center;
        text-align: center;
        color: white;
        background: linear-gradient(120deg, #43cea2 0%, #185a9d 100%);
        padding: 5rem 2rem;
    }

    .hero h1 {
        font-size: 2rem;
        font-weight: bold;
        color: #f8efd4;
        text-shadow: 1px 1px 3px rgba(0, 0, 0, 0.3);
        opacity: 0;
        transform: translateY(-20px);
        animation: fadeIn 1s forwards;
    }

    .hero p {
        font-size: 1rem;
        color: #ffffff;
        opacity: 0;
        animation: fadeIn 1s forwards 0.5s;
    }

    .hero img {
        width: 100%;
        max-width: 800px;
        padding-top: 1rem;
        opacity: 0;
        transform: scale(0.8);
        animation: fadeInImage 1s forwards 1s;
    }

    @keyframes fadeIn {
        to {
            opacity: 1;
            transform: translateY(0);
        }
    }

    @keyframes fadeInImage {
        to {
            opacity: 1;
            transform: scale(1);
        }
    }

    @media screen and (min-width: 768px) {
        .hero {
            height: 100vh;
        }

        .hero h1 {
            font-size: 3rem;
        }

        .hero p {
            font-size: 1.5rem;
        }
    }
</style>
